services:
  postgres:
    image: postgres
    environment:
      POSTGRES_USER: faroc
      POSTGRES_PASSWORD: changeit
      PGDATA: /data/postgres
    volumes:
      - postgres:/data/postgres
    ports:
      - "5432:5432"
    restart: unless-stopped

volumes:
  postgres:
